apiVersion: v1
kind: Service
metadata:
  name: nginx-np
spec:
  type: NodePort
  selector:
    # labels are the selectors
    project: roboshop
    environment: dev
    component: frontend
  ports:
  - protocol: TCP
    port: 80 # service port
    targetport: 80 #container port
    nodePort: 32323