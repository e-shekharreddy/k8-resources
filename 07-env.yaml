apiVersion: v1
kind: Pod
metadata:
  name: env-demo
spec:
  containers:
  - name: nginx
    omage: nginx
    env:
    - name: project
      value: roboshop
    - name: cource
      value: kubernetes

      